export default interface userData {
    id: number,
    name: string,
    lastName: string,
    contact: string,
    email: string,
    password: string,
    createdAt: string,
    updatedAt: string
}